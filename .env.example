# Back4App / Parse Configuration
# Copy this file to .env.local and fill in your actual keys
# NEVER commit .env.local to git

VITE_PARSE_APP_ID=your_application_id_here
VITE_PARSE_JS_KEY=your_javascript_key_here
VITE_PARSE_SERVER_URL=https://parseapi.back4app.com

# Clerk Authentication
# Get your publishable key from https://dashboard.clerk.com
# Go to: API Keys > Publishable key
VITE_CLERK_PUBLISHABLE_KEY=pk_test_YOUR_PUBLISHABLE_KEY

# Gated Chat Configuration
# Socket.io server URL (deployed on Railway/Render)
VITE_CHAT_SERVER_URL=https://wojak-chat.fly.dev

# Shared encryption key for E2E encrypted messages
# All 1% holders use the same key to encrypt/decrypt messages
VITE_CHAT_ENCRYPTION_KEY=your-shared-secret-key-here

# ==============================================
# CLOUDFLARE PAGES ENVIRONMENT VARIABLES
# ==============================================
# These are set in Cloudflare Pages dashboard, NOT in .env.local
# Go to: Pages > your-project > Settings > Environment Variables
#
# CLERK_DOMAIN=your-app.clerk.accounts.dev
#   - Your Clerk domain (without https://)
#   - Found in Clerk Dashboard > API Keys > Frontend API URL
#   - Used by backend to verify JWT tokens
#
# CHAT_JWT_SECRET=your-secret-key
#   - Secret for signing chat tokens (short-lived JWTs for Socket.io auth)
#   - Generate with: openssl rand -base64 32
#   - Must match the secret in your Socket.io server environment
#
# D1 Database Setup:
# 1. Create database: wrangler d1 create wojak-users
# 2. Add binding in Cloudflare Pages: Settings > Functions > D1 Database Bindings
#    - Variable name: DB
#    - D1 database: wojak-users
# 3. Run migration: wrangler d1 execute wojak-users --file=migrations/0001_create_users_tables.sql
