# Wojak Chat Server Environment Variables

# MongoDB connection string (MongoDB Atlas free tier works great)
# Example: mongodb+srv://user:pass@cluster.mongodb.net/wojak-chat
MONGODB_URI=

# Secret key for JWT verification (must match CHAT_JWT_SECRET in Cloudflare Pages)
# Generate with: openssl rand -base64 32
CHAT_JWT_SECRET=

# Port (Railway/Render set this automatically)
PORT=3001

# Allowed CORS origins (comma-separated)
ALLOWED_ORIGINS=https://wojak.ink,http://localhost:5173
